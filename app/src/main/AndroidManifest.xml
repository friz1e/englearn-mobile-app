<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.englishteacher">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Englearn"
        android:allowBackup="true"
        android:icon="@mipmap/englearn"
        android:roundIcon="@mipmap/englearn"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar.Light">

        <activity android:name=".PolishEnglishActivity" />
        <activity android:name=".EnglishPolishActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".LearnActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>